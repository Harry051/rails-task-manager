<h1 class="title">Your very own to do list 🧾🖋</h1>
<ul>
  <% @tasks.each do |task| %>
    <li class="list"> <%= link_to task.title, task_path(task) %> </li>
  <% end %>
</ul>
<br>
<p class="new-btn"><%= link_to "Want to make a new task?", tasks_new_path %></p>
